(load "senva/util.snv")

(define prolog::+environment+ nil)



(define prolog::assert
  (lambda (term)
	(! push prolog::+environment+ term)))

(define prolog::merge
  )

(define prolog::unify
  )

(define prolog::solve
  )

(define prolog::quest
  (lambda (expr binds cont)
	))

; cut, wildcard, var
